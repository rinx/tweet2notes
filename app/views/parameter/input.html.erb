<div class="hero-unit">

  <h1>Set Parameters</h1>

  <%= form_tag('/parameter/register', :method => 'post') do %>
    <label>notebook name</label>
    <%= text_field_tag 'notebook_name' %>
    <small class="help-block">保存先のノートブックを指定してください。</small>

    <label>tags</label>
    <%= text_field_tag 'tag_names' %>
    <small class="help-block">ノートに付けるタグを指定してください。</small>

    <%= link_to '&laquo; back'.html_safe, root_path, :class => 'btn' %>
    <%= submit_tag 'register &raquo;'.html_safe, :class => 'btn btn-primary' %>

  <% end %>

</div>
